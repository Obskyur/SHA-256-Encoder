<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="SHA_256_Encoder.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SHA_256_Encoder"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
        <TextBox Header="Username (SID):" PlaceholderText="Name" Margin="0,24,0,0"/>
        <PasswordBox Width="300" Header="Password:" PlaceholderText="Enter your password" PasswordChar="#" Margin="0,24,0,0"/>
        <Button x:Name="submitButton" Click="submitButton_Click" Margin="0,24,0,0">Submit</Button>

        <StackPanel Orientation="Horizontal" Margin="0,24,0,0">
            <Ellipse x:Name="authIcon" Width="20" Height="20" Fill="{Binding IsAuthenticated, Converter={StaticResource BoolToBrushConverter}}" Margin="0,0,8,0"/>
            <TextBlock Text="Is authenticated?"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0,24,0,0">
            <Ellipse x:Name="verifiedIcon" Width="20" Height="20" Fill="{Binding IsVerified, Converter={StaticResource BoolToBrushConverter}}" Margin="0,0,8,0"/>
            <TextBlock Text="Is verified?"/>
        </StackPanel>
    </StackPanel>
</Window>
